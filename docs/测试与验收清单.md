# 测试与验收清单

## 1. 环境验收
- MySQL 可用，库已导入
- Redis 可用
- AI 服务可启动且接口可访问（/predict、/match_points 等）
- 后端可启动，Swagger 可访问
- 前端可启动，地图可加载

## 2. 功能验收用例
### 2.1 设备管理
- 新增无人机成功
- 编辑无人机成功
- 删除无人机成功
- 列表查询正常

### 2.2 任务管理
- 新建任务（含航线点）成功
- 编辑任务成功
- 启动任务成功（状态变化、广播 task_start）
- 结束任务成功（状态变化、触发全局匹配流程）
- 查询/筛选任务列表正常

### 2.3 图像上传与识别链路
- 上传图片接口返回成功
- inspection_image 产生记录，original_image_url 有值
- AI 推理完成后 result_image_url 更新、status= DONE
- 若识别到病害：defect_detail 写入多条记录，任务 defect_count 增加

### 2.4 地图展示与详情
- 地图刷新后出现点位
- 点击点位能拉取详情列表
- 详情列表包含地址/路段信息（异步补全后）
- 原图/结果图链接可打开

### 2.5 实时告警与遥测
- 采集控制台连接 WebSocket 成功
- 收到 telemetry 消息并显示
- 识别到病害时收到 new_defect 告警并显示结果图链接

## 3. 性能与稳定性
- 连续上传 N 张图片（如 30 张），系统无明显崩溃/卡死
- 地图瓦片多次移动缩放后仍可加载（Redis 缓存生效）